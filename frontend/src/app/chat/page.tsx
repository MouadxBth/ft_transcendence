import ChatSideBar from "@/components/chat/ChatSideBar";

const ChatPage = () => {
	return (
		<article className="w-3/4 h-full flex items-center justify-center">
			Start a new Conversation/Channel
		</article>
	);
};

export default ChatPage;
 